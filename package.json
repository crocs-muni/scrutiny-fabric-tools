{
  "name": "scrutiny-fabric-tools",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "//biome": "Pinned exactly, not ^: the D12 crypto-import ban is a biome nursery rule, and a minor bump that promotes or renames it would silently stop enforcing it. test/no-crypto.test.ts backs the ban independently.",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome check --write .",
    "attw": "pnpm -r attw",
    "rules:gen": "node tools/gen-rules.mjs",
    "rules:check": "node tools/gen-rules.mjs --check",
    "verify": "pnpm lint && pnpm typecheck && pnpm rules:check && pnpm build && pnpm attw && pnpm test"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.11",
    "typescript": "^5.8"
  },
  "//pnpm": "vitest 3.2.7 resolves vite 5.4.21 transitively, which carries a high-severity server.fs.deny bypass and two path-traversal advisories; esbuild <0.25 lets any website reach the dev server. All are dev-only — vitest is a devDependency and the package ships only `diff` at runtime — but overriding is cheap and keeps `pnpm audit` clean so a real advisory is not lost in the noise. Drop these once vitest depends on patched versions directly.",
  "pnpm": {
    "overrides": {
      "vite": "^6.4.3",
      "esbuild": "^0.25.0"
    }
  },
  "workspaces": ["packages/*"]
}
