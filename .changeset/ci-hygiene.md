---
"@scrutiny-fabric/core": patch
---

CI hygiene: new verify-examples job (examples were never installed or tested), job timeouts, changeset gate base-ref fix, and root config trimming (changeset .gitignore footgun, duplicate workspaces field, duplicate attw dep, redundant biome ignore, grouped monthly Dependabot).
